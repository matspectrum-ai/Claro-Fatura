# Claro Fatura

Baseline do sistema atual e reimplementação compatível em stack leve.

> A branch `main` preserva o comportamento do snapshot original. A migração deve manter produto, UI, fluxos, banco e contratos existentes, alterando apenas a implementação técnica.
